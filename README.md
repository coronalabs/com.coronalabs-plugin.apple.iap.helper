Sources for the plugin `plugin.apple.iap.helper`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.apple.iap.helper" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
